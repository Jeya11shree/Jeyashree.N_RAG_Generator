# rag-multimodal

Starter layout for a Retrieval-Augmented Generation (RAG) multimodal project.

- `src/` contains core modules
- `sample_data/` contains example docs and assets
- `db/` is the default persistence directory (gitignored)

See `docs/design.md` for architecture notes.
