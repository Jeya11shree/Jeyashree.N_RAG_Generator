# Accessibility Requirements for Signup

- All form controls must have associated `<label>` elements.
- Error messages must be announced via `aria-describedby`.
- Color contrast for critical text/buttons must meet WCAG AA.
- Focus order should follow the visual flow; Skip links not required for single-page form but ensure keyboard navigation.
